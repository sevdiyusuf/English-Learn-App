{
  "name": "firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore Security Rules unit tests for Sprint 4D",
  "scripts": {
    "test": "jest --testPathPattern=firestore.rules.test.js --forceExit",
    "test:emulator": "firebase emulators:exec --only firestore \"npx jest --testPathPattern=firestore.rules.test.js --forceExit\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "jest": "^29.7.0"
  }
}
